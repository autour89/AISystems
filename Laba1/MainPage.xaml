<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodel="clr-namespace:Laba1.ViewModels"
             x:DataType="viewmodel:MainViewModel"
             x:Class="Laba1.MainPage">

    <ScrollView>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="100"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Entry Grid.Row="0" Placeholder="First name"
                  Text="{Binding FirstName}"/>
            <Entry Grid.Row="1" Placeholder="Last name"
                  Text="{Binding LastName}"/>

            <Label Grid.Row="2" VerticalTextAlignment="Center" Text="{Binding FullName}"/>

            <Button Grid.Row="3"
                Text="Click me"
                SemanticProperties.Hint="Counts the number of times you click"
                HorizontalOptions="Center" />

            <ActivityIndicator Grid.Row="4" />

        </Grid>
    </ScrollView>

</ContentPage>

